apiVersion: v1
kind: Secret
metadata:
  name: agentscan-secrets
  namespace: agentscan
type: Opaque
data:
  # Database credentials (base64 encoded)
  DB_USER: YWdlbnRzY2Fu  # agentscan
  DB_PASSWORD: ""  # To be set during deployment
  
  # Redis password
  REDIS_PASSWORD: ""  # To be set during deployment
  
  # JWT signing key
  JWT_SECRET: ""  # To be set during deployment
  
  # OAuth credentials
  GITHUB_CLIENT_ID: ""
  GITHUB_CLIENT_SECRET: ""
  GITLAB_CLIENT_ID: ""
  GITLAB_CLIENT_SECRET: ""
  
  # Notification credentials
  SLACK_WEBHOOK_URL: ""
  TEAMS_WEBHOOK_URL: ""
  SMTP_PASSWORD: ""
---
apiVersion: v1
kind: Secret
metadata:
  name: agentscan-tls
  namespace: agentscan
type: kubernetes.io/tls
data:
  tls.crt: ""  # TLS certificate (base64 encoded)
  tls.key: ""  # TLS private key (base64 encoded)